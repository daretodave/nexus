---
description: Propose new kit phases from accumulated signals. Candidates only; /oversight promotes.
---

You are invoked under the `expand` skill — posture-gated plan
growth. Read `skills/expand.md` end to end before touching
anything else.

Argument handling:
- No argument → full signal read + up to 3 candidates.
- `dry-run` → print candidates, file nothing.

Procedure: §5. Hard rules: §6. Failure modes: §7. Signals:
audit clusters, critique clusters, triage patterns, sibling
lessons, platform drift. Never write to the build plan
directly.

When invoked under `/loop` or `/march`, the user is not
present. After commit + push (or a clean no-candidates exit),
return cleanly.

Argument: $ARGUMENTS
